node_modules/
*.log
.DS_Store
backups/
!backups/.gitkeep
palette/preview.html

# opencode IDE/agent config (entorno de desarrollo, no del proyecto)
.opencode/
opencode.json
opencode.jsonc

# Estado efímero de ejecución del pipeline AIEOS (no versionar).
# .claude/agent-memory/ SÍ se versiona (memoria de proyecto persistente).
.claude/pipeline-state.json

# Capturas debug / screenshots de revisión estética (no son entregables)
/2026*.png
/ALL_WIDGETS.png
/COMPARE.png
/final4.png
/launchers_check.png
